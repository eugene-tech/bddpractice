1. При создании новых POMs и stepDefinitions как их взаимосвязать с уже существующими степами?
Пример :
добавлены RatesTab, в которых могут быть реализованны шаги точно так же, как и в тех,что были изначально. 
--При этом, если степы повторяются : [Given main page is opened], который инициализируется в старом и новом классе , 
то при запуске он выдает ошибку "Дубликат". 
Каким образом можно вызвать [Given main page is opened] из одного SD в другом?

2. Как можно красиво реализовать клики в POMs , используя @FindBy ?
На данный момент я реализую клики в SD, испоьзуя следующее :
driver.findElement(By.xpath("//*[@id='menu-top-collapse']/ul/li[6]")).click();

Попытка реализации - 
//    @FindBy(xpath = "//*[@id='menu-top-collapse']/ul/li[2]")
//    public List<WebElement> linkRatesBtn;

Но что делать дальше, как инициализировать?

3. В некоторых местах используется *[@id='convertor'] , правильное ли использование * или есть альтернатива?

4. На странице https://www.curs.md/en/grafic_evolutii у нас имеется "Relative to: " , можем ли мы это протестировать (смену языка)?

5. На странице https://www.curs.md/ro/convertor присутствует "Ce trebuie să știți despre curs de schimb în numerar?" , но при этом у нас нет
этого для других языков. Необходимо ли это тестировать?

