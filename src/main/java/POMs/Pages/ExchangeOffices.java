package POMs.Pages;


import POMs.AbstractPOM;
import org.openqa.selenium.WebDriver;

public class ExchangeOffices extends AbstractPOM {
    public ExchangeOffices(WebDriver driver) {
        super(driver);
    }
}
