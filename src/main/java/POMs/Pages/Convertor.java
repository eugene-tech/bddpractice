package POMs.Pages;


import POMs.AbstractPOM;
import org.openqa.selenium.WebDriver;

public class Convertor extends AbstractPOM {
    public Convertor(WebDriver driver) {
        super(driver);
    }
}
