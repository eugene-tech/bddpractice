package POMs.Pages;


import POMs.AbstractPOM;
import org.openqa.selenium.WebDriver;

public class Currencies extends AbstractPOM {
    public Currencies(WebDriver driver) {
        super(driver);
    }
}
